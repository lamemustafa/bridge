# Goal 4 — Address every open Codex review thread on PR #228

Same worktree/branch/rules as the earlier briefs (Rust 1.96 on PATH; sealed-surface edits allowed only where already authorised in `GOAL3B-BRIEF.md`, followed by the documented reseal; commit as you go; do not push). The PR received 25 review threads. Fix each one properly — no suppression, no "won't fix" unless the finding is factually wrong, in which case write the counter-evidence with file:line in the report. Add a regression test for every behavioural fix. Reply text for each thread goes in `docs/agent/GOAL4-REPORT.md` as a table: `path:line | finding | fix commit | test | note`.

## P1 findings (must fix)

1. **`src-tauri/src/agent_import.rs:706` — import correlation cannot rely on client REMOTEID readback.** `docs/tally/IMPLEMENTATION_GUIDE.md` §3.3a records that Tally overwrites a client-supplied `REMOTEID`, so `verify_import` currently reports every real import as `not_found`. Redesign correlation: keep `REMOTEID=bridge_txn_id` on the import file for Tally-side idempotency (the vault's 2026-07-30 observation is that re-import with the same REMOTEID upserts), but make the **observable** markers (a) a narration tag `[BRIDGE:<bridge_txn_id>]` appended to the narration (documented, and stripped from any user-facing narration display), and (b) the accounting fingerprint `(date, voucher_type, sorted (ledger, signed amount) set)`. `verify_import` matches on tag first, fingerprint second, and reports `posted_verified` / `posted_divergent` / `not_found` / `duplicate_fingerprint` accordingly, stating which marker matched. If the guide's §3.3a claim conflicts with the vault observation, record both in the report and implement so that either behaviour verifies correctly. Also read back `VOUCHERNUMBER` and `GUID`/`MASTERID` for the proof.
2. **`agent.rs:1020` — TDL injection via `ledger`/filter values.** Introduce a `TdlStringValue` newtype (or reuse the repository's existing safe-value types used by the voucher runtime for dates) that rejects quotes, `$`, `#`, `:`, control characters and any character outside a documented safe set before interpolation into any `<FILTERS>`/`$$` expression. Never rely on XML escaping alone. Test with a quote, a `$$` function fragment and a colon.
3. **`agent.rs:184` — route custom reads through the identity-bracketed runtime path.** `post_read` dispatches via a separate transport after `verified_company`; move voucher, change-feed and verification reads onto the runtime's single bracketed dispatch (identity re-verified around the data read, response-bound company evidence). If a new runtime entry point is needed in `tally/runtime.rs`, that edit is within the Goal 3b authorisation; reseal afterwards.
4. **`agent.rs:980` — redaction must apply centrally to every response shape** (`name` in ledger masters and changed masters, changed voucher rows, proof outputs) before hashing and output. Test: with `mask_parties`, no response from any tool contains a raw party name from the simulator fixture.
5. **`agent_import.rs:206` — normalise verification window dates to `YYYYMMDD` at the boundary** and store the normalised values; test that a built batch verifies with the same bounds used to render the file.
6. **`agent_import.rs:692` — reject invalid verification envelopes**: empty body, error envelope, non-voucher XML → typed protocol failure, never a proof with everything `not_found`.
7. **`agent.rs:1132` — `vouchers` must return the fetched ledger entries** (ledger name, signed amount, polarity); fail closed if the entries are absent from a response that should contain them.
8. **`agent.rs:740` — `ledger_movement` opening balance**: when `from` is later than books-from, derive the opening at `from` (either a ledger read scoped to `from`, or include pre-window movement from books-from), and document the method in `evidence.method`.
9. **`agent.rs:803` — signed arithmetic**: keep Tally's convention (debit negative, credit positive) end-to-end; closing = opening − debit magnitude + credit magnitude on the signed scale. Test with a debit opening and a debit movement.
10. **`agent.rs:619` — `changed_since` truncation**: detect when either collection exceeded `max_rows`, set `truncated: true`, and provide cursor/pagination (`offset` or "next alter_id") so nothing is silently skipped.
11. **`agent.rs:114` — Windows data directory**: resolve a durable per-user app-data directory on Windows (`%LOCALAPPDATA%\Bridge\agent` or the directory Bridge's Tauri app already uses); never the temp dir.
12. **`agent.rs:607` — company high-water marks**: parse the `Company` collection rows and select the row whose GUID equals the verified GUID; fail closed if absent (`TEST_CORPUS.md` §7.1 says the first row may be another company).
13. **`agent.rs:1137` — parser errors are not completion**: propagate typed parse errors from the voucher and changed-master parsers; validate the expected envelope before reporting `evidence.state = "complete"`.
14. **`agent.rs:397` — egress write failures must be in-band**: if the receipt cannot be written, the tool call fails (or returns `egress_recorded: false` with an error) rather than reporting success; `egress_log` distinguishes unreadable from empty.
15. **`agent.rs:808` — evidence must bind the data-producing reads**: combine request/response commitments from every runtime read that produced the result (ledgers, vouchers, outstandings pair, party masters), not just the company list.
16. **`packaging/mcpb/manifest.json:12` — binaries**: add a packaging script (`scripts/package-mcpb.mjs` or a cargo alias) that builds `bridge_mcp` for the host and copies it to `packaging/mcpb/bin/`, and document that `.mcpb` assembly requires that step; do not commit binaries.
17. **`agent.rs:573` — separate voucher and master checkpoints**: `changed_since` accepts and returns `voucher_alter_id` and `master_alter_id` independently.
18. **`agent_import.rs:196` — atomic transaction admission**: hold an interprocess lock (`fs2`/`fd-lock`-style advisory lock on a lock file in the data dir) across read-check-create-append in `build_import_xml`.
19. **`agent.rs:1263` — malformed `tools/call` must not terminate the server**: return a JSON-RPC error and continue.
20. **`docs/agent/GOAL1-BRIEF.md:3` and `GOAL2-BRIEF.md` — remove developer-specific absolute paths**; use repository-relative wording ("this worktree").

## P2 findings (fix)
21. `agent.rs:901` — `list_companies` must label rows `verified_tuple` only when the complete-tuple invariant holds; otherwise `incomplete_tuple` with the missing field named.
22. `agent.rs:1249` — negotiate the MCP protocol version from the client's `initialize` params (support `2025-06-18` and `2024-11-05`; return the requested one if supported, else the latest supported).
23. `agent.rs:1020` — the `ledger` filter must match any ledger touched by the voucher (entry membership), or be renamed `party_ledger`; do the former.
24. `agent.rs:692` — apply `direction` consistently to totals, ageing, top parties and unallocated, or remove the parameter; do the former.
25. `agent.rs:347` — derive the human summary from the error state ("read withheld" when `isError`).

Finish with: fmt, full `cargo test --manifest-path src-tauri/Cargo.toml --workspace`, clippy for the bin with `-D warnings`, `corepack pnpm test`, the compatibility gate (reseal if any sealed file changed), and `docs/agent/GOAL4-REPORT.md` with the table above and honest gaps.