# Goal 3 — Make the read tools real: reuse Bridge's existing outstandings and ledger pipelines

Same worktree/branch/rules as `docs/agent/GOAL1-BRIEF.md`. Review findings on the Goal 1 code that this goal must fix (all in `src-tauri/src/agent.rs`):

1. `outstandings` returns raw native bill rows and stubs totals/ageing/unallocated, although Bridge already computes exactly these in the Tauri path (`fetch_tally_outstandings` in `src-tauri/src/commands.rs` → `TallyRuntime::fetch_outstandings` in `src-tauri/src/tally/runtime.rs` → `bridge_tally_protocol::native_outstandings` + `outstandings_shared`, including the ledger-residual pairing, both ageing bases, Partial reason codes and base-currency detection). Refactor so the MCP tool calls the same non-Tauri function the command uses (extract a plain async fn if it is currently tangled with Tauri `State`; keep the command's behaviour byte-identical and its tests green). The tool must return: receivable/payable totals, four ageing buckets for the requested basis (`bill_date`|`due_date`), top-N parties by exposure, unallocated count and amount, and the exact `Partial{reason_code}` when the pipeline refuses — never a made-up total. Add a simulator test that asserts totals equal the sum of returned open bills for a fixture where the pipeline is complete, and a test for a Partial refusal.
2. `ledger_movement` is a stub returning `ledgers: []`. Bridge's native ledger read already fetches opening/closing per ledger (see `List of Ledgers` profile with `OPENINGBALANCE`/`CLOSINGBALANCE` in `native_outstandings/request.rs`, and the vault-proven `TBALOPENING`/`TBALCLOSING` fields — search the tree for `TBALCLOSING`). Implement: per-ledger opening, debit total, credit total, closing over the literal-date window, computed from the windowed voucher entries returned by the existing voucher profile (sum `ISDEEMEDPOSITIVE` sides with `ExactDecimal`), plus `parent` group and the count of vouchers touching the ledger. If TBAL fields are available through a non-sealed profile, add a `trial_balance` tool returning name, parent, opening, closing with the Dr=Cr self-check result; otherwise document why not. Tests: a simulator fixture with three vouchers across two ledgers; assert the movement arithmetic and that the window excludes out-of-range vouchers.
3. `changed_since` writes `rows_returned: 0` to the egress receipt regardless of rows returned — fix, and add an assertion in the existing egress test. Also return `masters` changed (ledgers/groups with `ALTERID > mark`) using the ledger master profile, and read the company-level `ALTVCHID`/`ALTMSTID` via the company extent profile if it exposes them (search for `ALTVCHID` in the tree; the H2 plan §8.7 says the extent probe returns them). Keep the honest `deletion_detection: unsupported` note.
4. `ledger_masters` with `fields: "compliance"` is "unsupported"; the party-ledger-master export already fetches GSTIN/PAN/MSME/bank/IFSC/email/phone/state/address (`export_party_ledger_master` command → `reports/party_ledger_master` / the master fetch list in `native_outstandings/request.rs`). Reuse it; keep redaction applying to party names.
5. `tools/list` schemas: make each tool's `inputSchema` a real JSON Schema with required fields, enums and defaults (Claude Desktop shows these to the user); add one-line `description` per tool that says what evidence comes back.

Quality gates as before (Rust 1.96 on PATH: `export PATH=$HOME/.rustup/toolchains/1.96.0-aarch64-apple-darwin/bin:$PATH`), sealed-surface files untouched, existing tests green, clippy clean for the bin, `docs/agent/README.md` updated with real sample outputs, and `docs/agent/GOAL3-REPORT.md` with test output and honest gaps. Commit as you go; do not push.