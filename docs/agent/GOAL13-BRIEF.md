# Goal 13 — Tenth-round Codex review thread on PR #228 (1 finding, 2026-09-05 16:08 UTC) plus one residual from Goal 12

Same worktree, branch and rules as `GOAL12-BRIEF.md` (Rust 1.96 via `rustup run 1.96.0`, one regression test per fix, commit per fix, do not push, sealed edits only where `GOAL3B-BRIEF.md` authorises them plus the documented reseal, no won't-fix without file:line counter-evidence). Report as `docs/agent/GOAL13-REPORT.md` in the Goal 12 format with a reply paragraph per thread. Finish with the workspace suite, clippy on `bridge_mcp` with `-D warnings`, the compatibility gate with the five CI arguments, the Node/Vitest part of `corepack pnpm test`, and Playwright via the direct-vite fallback. Both items are accepted.

1. **`agent_import.rs:795` — propagate verification decode errors.** The import read-back parser matches `(Some(current), Ok(value))` and silently drops a fragment whose entity fails to decode, so `A&bogus;B` becomes `AB` and can satisfy an expected `AB` fingerprint. Propagate the typed error (`import_verification_export_invalid`) from both text and entity-reference fragments so malformed wire evidence can never contribute to `posted_verified`. Test with an unknown entity in a ledger name and in the narration.
2. **Residual from Goal 12 item 4 — compliance-branch evidence in `ledger_masters`.** The compliance branch currently attaches the evidence of a second, plain ledger export rather than of the party-ledger-master read that supplied the returned fields (`name_on_pan`, GSTIN, bank details), and performs that extra read only for the hash. Make the compliance read itself evidence-bearing (a `..._with_evidence` variant in the sealed runtime, minimal edit and reseal), combine that evidence, and drop the extra read. Test that the compliance evidence changes when the compliance response changes and that only one ledger read is issued.
