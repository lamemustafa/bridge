# Goal 3b — Goal 3 with sealed-surface authorisation

Authorisation (owner-delegated, branch `feat/agent-connector` only, 2026-09-04): you MAY edit the sealed files named in `docs/agent/BLOCKERS.md` (`src-tauri/src/lib.rs`, `src-tauri/src/tally/mod.rs`, `src-tauri/src/tally/runtime.rs`, and `src-tauri/src/commands.rs` if needed) with the **smallest** change that exposes a library-facing, identity-safe, non-Tauri read adapter — then you MUST run the documented reseal sequence from `docs/release-process.md` (`rehash-surface` → `seal-surface` → `repoint-matrix`, exact commands there) so the CI seal check passes, and record in `GOAL3-REPORT.md` exactly which sealed files changed, why, and the before/after hash counts. Every compatibility-matrix cell is `unknown`, so resealing changes no public claim; say so in the report. Do not weaken any invariant: identity construction must still reject Missing/DuplicateTuple/DisplayScopeAmbiguous; reads stay loopback-only and paired; `Complete`/`Partial` contract unchanged.

Design constraint: prefer moving `agent.rs` into `bridge_lib` as `pub mod agent` (so the bin becomes a thin wrapper calling `bridge_lib::agent::run_stdio()`), and add one `pub` constructor on `VerifiedCompanyIdentity` that takes the observed company tuple and runs the same validation as the private path (or make the existing `from_observed_companies` `pub` with docs). Keep the Tauri command untouched if a shared async function already exists on `TallyRuntime`; otherwise extract one and make the command call it (byte-identical behaviour, its tests green).

Then complete every item of `docs/agent/GOAL3-BRIEF.md` (real `outstandings` via `TallyRuntime::fetch_outstandings` with totals/ageing/unallocated/Partial; `ledger_movement` and, if TBAL fields are reachable, `trial_balance`; the `changed_since` egress row-count fix + masters + ALTVCHID/ALTMSTID if the extent profile exposes them; compliance fields in `ledger_masters`; real JSON schemas in `tools/list`). Tests as specified there. Quality gates: `export PATH=$HOME/.rustup/toolchains/1.96.0-aarch64-apple-darwin/bin:$PATH`; `cargo fmt --check`, `cargo test --manifest-path src-tauri/Cargo.toml` for the bin AND the existing lib/command tests, `cargo clippy --bin bridge_mcp -- -D warnings`, `corepack pnpm test` if it runs in this environment, and the seal verification the CI runs (find it in `.github/workflows/ci.yml` and `scripts/`). Commit as you go; do not push. Update `docs/agent/README.md` with real sample outputs and finish with an honest `GOAL3-REPORT.md`.