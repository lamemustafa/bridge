# Goal 11 — Eighth-round Codex review threads on PR #228 (11 findings, 2026-09-05 10:58 UTC)

Same worktree, branch and rules as `GOAL10-BRIEF.md` (Rust 1.96 on PATH via `rustup run 1.96.0`, one regression test per fix, commit per fix, do not push, sealed edits only where `GOAL3B-BRIEF.md` authorises them plus the documented reseal, no won't-fix without file:line counter-evidence). Report as `docs/agent/GOAL11-REPORT.md` in the Goal 10 format with a reply paragraph per thread. Finish with the workspace suite, clippy on `bridge_mcp` with `-D warnings`, the compatibility gate with the five CI arguments, the Node/Vitest part of `corepack pnpm test`, and Playwright via the direct-vite fallback. All eleven are accepted; three are P1 and go first.

## P1
1. **`agent.rs:1441` — future-due bills belong in the first ageing bucket.** A bill whose `age_days` is `None` (ageing date after `as_of`) is still open exposure; map it to `days_0_30` exactly as the native computation does, keep the per-bill age nullable, and add a test that buckets sum to totals with one future-due bill present.
2. **`agent.rs:2061` — changed masters without a name fail closed.** A ledger or group row with a valid ALTERID but a missing or empty NAME fails the scan with `change_row_name_invalid` before any cursor is built. Test.
3. **`agent_import.rs:294` — corroborate the verification window before certifying.** Before absence or uniqueness is used as posting proof: (a) every returned row's date must lie inside the batch window, else `window_not_honoured`; (b) read the window a second time and require the identical set of (GUID, ALTERID) pairs, else `verification_incomplete` with `window_not_corroborated`; (c) if the row count equals the configured `max_rows` or any Tally truncation signal is present, `verification_incomplete` with `window_possibly_truncated`. Only a corroborated window may yield `posted_verified`. Test (a), (b) and (c).

## P2
4. **`agent.rs:1655` — trim changed-feed arrays under the byte cap.** The trimmer must handle `result.vouchers` and `result.masters`: drop trailing rows from the larger array, set that axis's `next_*_alter_id` to the maximum ALTERID of the surviving rows, set `truncated: true` and `checkpoint_advanceable: false`. Test.
5. **`agent.rs:947` — combine outstandings read evidence.** The currency-detection read and the outstandings read(s) must contribute their request hash, response hash and bytes to the returned evidence; if the runtime helper does not expose them, add a variant that does (sealed-edit rule applies). Test that evidence changes when the bill response changes.
6. **`agent.rs:2050` — entity fragments in changed-master fields.** Apply the Goal 10 decode-and-append fix to the changed-masters parser (name and parent), failing closed on decode errors. Test `R&amp;D`.
7. **`agent.rs:1927` — master checkpoint domain must match the scanned collections.** Decision: do not scan every master type. Replace the ALTMSTID snapshot for the master axis with a domain-matched high-water: the maximum ALTERID over the Ledger and Group collections, read by a dedicated ALTERID-only collection query at snapshot time; corroboration then compares the page maximum against that domain maximum. Document in the README that master change detection covers ledgers and groups only and that other master types are out of scope. Test that a change to an unsupported master type no longer stalls the feed.
8. **`agent.rs:1801` — reject zero-sized limits.** `limit` and `top` must be at least 1 at the argument boundary (`pagination_invalid`), matching the published schemas. Test.
9. **`agent_import.rs:255` — no orphan import files.** If the ledger append fails after the XML was written, delete the XML file and return the error; if deletion also fails, return `import_file_orphaned` naming the path. Test with an unwritable ledger path.
10. **`agent_import.rs:284` — canonical batch GUID.** Persist the GUID in canonical lowercase form and compare case-insensitively at verification, matching the identity type. Test mixed-case round trip.
11. **`agent.rs:1524` — shared lock on egress-log tail reads** so a reader cannot observe a writer's partial line. Test a read concurrent with an append.
