# Goal 9 — Sixth-round Codex review threads on PR #228 (6 findings, 2026-09-05 08:13 UTC)

Same worktree, branch and rules as `GOAL8-BRIEF.md` (Rust 1.96 on PATH, one regression test per fix, commit per fix, do not push, sealed edits only where `GOAL3B-BRIEF.md` authorises them plus the documented reseal, no won't-fix without file:line counter-evidence). Report as `docs/agent/GOAL9-REPORT.md` in the Goal 8 format with a reply paragraph per thread. Finish with the workspace suite, clippy on `bridge_mcp` with `-D warnings`, the compatibility gate with the five CI arguments, the Node/Vitest part of `corepack pnpm test`, and Playwright via the direct-vite fallback. All six are accepted; five are P1.

1. **`agent.rs:1574` — redact every party-bearing field under `mask_parties`.** Replace the key allowlist with an explicit per-shape redaction: compliance `name_on_pan`, `bank_account_holder_name` and any other name-like compliance field; `validate_masters` `exact_live_spelling` and `candidates`; party fields in open bills, unallocated parties, top parties, voucher `party_ledger_name` and entry ledger names where they are party ledgers. Prefer a typed `PartyName` newtype (or a marker in the JSON builder) so the central redactor cannot miss a new field; add a test that walks every tool's sample response under `mask_parties` and asserts no known party string survives.
2. **`agent_import.rs:896` — narration-tag matches must postdate the mark.** A `[BRIDGE:<txn>]` match counts only if its `ALTERID` is greater than the persisted company high-water voucher mark; an older tagged voucher is `not_attributable` with reason `tag_precedes_pre_import_voucher_mark`. Test: same tag below the mark → not_attributable; above → posted_verified.
3. **`agent.rs:988` — unobserved ledger opening fails closed.** When `OPENINGBALANCE` is absent and the window starts after books-from, do not substitute zero: return the ledger row with `opening: null`, `closing: null`, `state: "partial"`, `reason: "opening_balance_not_observed"`, and mark the tool result partial; never present a fabricated numeric opening. Test.
4. **`agent.rs:2053` — missing or non-string `params.name` is an in-band JSON-RPC error**, not a `run_stdio` exit. Test that a malformed request is followed by a served request in the same session.
5. **`agent.rs:854` — apply `direction` to every returned view.** For `receivable` or `payable`, derive totals, ageing buckets, top-party ranking, open bills and unallocated parties from the selected side only (the bill-level rows carry direction; the ranking already comes from bills). If ageing cannot be recomputed per side from what the report exposes, omit `ageing_buckets` for a single-direction request with `ageing_basis: "not_available_for_single_direction"` rather than copying the combined figures. Test a payable-only request against mixed bills.
6. **`agent.rs:643` — corroborate empty voucher windows properly.** An empty first read is corroborated only by a meaningful control: (a) the one-day-widened read returns at least one row, all of which lie outside the original window → the empty result is `complete`; (b) the widened read returns any row inside the original window → fail with `window_contradicted`; (c) the widened read is also empty → consult the company high-water: `altvchid == 0` (no vouchers at all) → `complete` with `reason: "company_has_no_vouchers"`, otherwise `partial` with `empty_uncorroborated`. Test all three branches.
