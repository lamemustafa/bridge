# Goal 12 — Ninth-round Codex review threads on PR #228 (5 findings, 2026-09-05 15:09 UTC) plus one residual from Goal 11

Same worktree, branch and rules as `GOAL11-BRIEF.md` (Rust 1.96 via `rustup run 1.96.0`, one regression test per fix, commit per fix, do not push, sealed edits only where `GOAL3B-BRIEF.md` authorises them plus the documented reseal, no won't-fix without file:line counter-evidence). Report as `docs/agent/GOAL12-REPORT.md` in the Goal 11 format with a reply paragraph per thread. Finish with the workspace suite, clippy on `bridge_mcp` with `-D warnings`, the compatibility gate with the five CI arguments, the Node/Vitest part of `corepack pnpm test`, and Playwright via the direct-vite fallback. All six are accepted; four are P1 and go first.

## Residual from Goal 11 (treat as P1)
0. **`agent.rs` changed-masters parser — whitespace around entity fragments is trimmed.** `parse_agent_changed_masters` runs with `trim_text(true)`, so `Income &amp; Expense` decodes to `Income&Expense` (the Goal 11 test `changed_master_parser_decodes_entity_fragments` asserts this wrong value). Switch that parser to `trim_text(false)` the way the import parser and voucher parser already do, keep whitespace-only text between elements out of fields (the tag is cleared on End), and fix the test to expect `Income & Expense`. Check `parse_master_domain_high_water` and `validate_agent_envelope` are unaffected.

## P1
1. **`agent.rs:1240` — pin ledger openings to the accounting window.** The date-less LedgersV1 read returns OPENINGBALANCE for Tally's displayed period, not for BOOKSFROM (protocol reference §5.5). Either render the ledger export with SVFROMDATE pinned to BOOKSFROM (and verify the response's period), or fetch the opening directly at the requested `from` and drop the pre-window voucher aggregation. Prefer the second if the protocol reference marks it verified; otherwise the first with a period assertion that fails closed (`opening_period_not_honoured`). Test both a matching and a mismatching period.
2. **`agent.rs:2317` — changed vouchers need a stable identity.** A changed voucher row must carry a non-empty GUID or MASTERID; otherwise fail the scan with `change_row_identity_invalid` before any cursor is built. Test.
3. **`agent.rs:1017` — unallocated balances in the top-party ranking.** Aggregate per party across the selected-direction open bills and the selected-direction unallocated residuals before ranking; expose both components (`billed`, `unallocated`) per party alongside the total. Test a party whose exposure is wholly unallocated ranking first.

## P2
4. **`agent.rs:694` — ledger reads in `ledger_masters` evidence.** Combine the evidence of the basic or compliance ledger read (request hash, response hash, bytes) into the returned evidence, via an evidence-bearing runtime read (sealed-edit rule applies if it touches `runtime.rs`). Test that the evidence changes when the ledger response changes.
5. **`agent.rs:2520` — record egress after final framing.** Move the egress receipt to after JSON-RPC framing and the final trim (or update it from the exact serialised bytes written to stdout), so the receipt describes the response actually emitted, including the `agent_response_too_large` replacement. Test a case where only the envelope pushes the result over the cap.
