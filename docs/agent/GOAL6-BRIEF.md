# Goal 6 — Third-round Codex review threads on PR #228 (6 findings, 2026-09-04 11:38 UTC)

Same worktree, branch and rules as `GOAL5-BRIEF.md` (Rust 1.96 on PATH, one regression test per fix, commit per fix, do not push, sealed edits only where `GOAL3B-BRIEF.md` authorises them plus the documented reseal, no won't-fix without file:line counter-evidence). Report as `docs/agent/GOAL6-REPORT.md` in the Goal 5 table format with a reply paragraph per thread. Finish with the workspace suite, clippy on `bridge_mcp` with `-D warnings`, the compatibility gate, and `corepack pnpm test` (if Playwright cannot start, run `corepack pnpm exec playwright test` directly and record the outcome).

## Findings and the decision for each

1. **`agent.rs:45` — reject unknown `BRIDGE_AGENT_REDACTION` values.** Unset → default preset; any other unrecognised value → typed startup error (`redaction_setting_invalid`) and the server refuses to serve. Test both.
2. **`agent.rs:655` — pin change-feed pages to a stable snapshot and use AlterID cursors.** The first page records the company high-water snapshot (`altvchid`, `altmstid`); every subsequent page requests `ALTERID > checkpoint AND ALTERID <= snapshot` in `<FILTERS>` and orders by ALTERID, so rows entering after the snapshot cannot shift the window. Replace the positional `next_offset` for this tool with `next_voucher_alter_id` / `next_master_alter_id` = the maximum ALTERID returned on the page (never the company high-water) while the page is truncated, and the snapshot value only when the page is complete and corroborated. Keep `checkpoint_advanceable` semantics from Goal 5. Test: simulate a new row arriving between page 1 and page 2 and assert no row is skipped.
3. **`agent.rs:1272` — fail closed on malformed change-row AlterIDs.** Any ledger/group/voucher row in a change scan with an empty or non-numeric ALTERID fails the scan with `change_row_alterid_invalid`; no checkpoint is returned. Test with one bad row.
4. **`agent_import.rs:631` — voucher numbers.** Do not make a manual number mandatory: Tally's numbering method belongs to the voucher type, and Bridge's duplicate protection is the narration marker plus fingerprint, not the number. Instead: add an optional, validated `voucher_number` per input voucher (non-empty, ≤ 32 chars, no control characters, no `$`), render it as `<VOUCHERNUMBER>` when present, echo it in the read-back proof, and document in `docs/agent/README.md` that numbering otherwise follows the voucher type's configuration. If the reviewer's scenario (type configured Manual with "prevent duplicates") is real, a supplied number is what makes Tally reject the collision. Test: rendered when supplied, absent when not, invalid rejected.
5. **`agent_import.rs:205` — gate import generation until live evidence exists.** Accept. `build_import_xml` and `verify_import` are hidden from `tools/list` and refuse calls with `import_unverified_on_live_tally` unless `BRIDGE_AGENT_ENABLE_IMPORT=1` is set. When enabled, every `build_import_xml` result carries `"live_evidence": "none_recorded"` plus the pointer to `docs/agent/GOAL2-REPORT.md` until a later goal records a live import read-back, at which point the gate is removed. Update README, the connector brief and the MCPB manifest description accordingly. Test: tool absent and refused by default; present when enabled with the warning field.
6. **`agent.rs:725` — report truncated open-bill results.** Paginate `open_bills` with the standard `offset`/`limit` (bounded by `max_rows`) and set `truncated: true` with `next_offset` when bills remain; keep `top` for the party ranking only and say so in the schema description. Test: more bills than the limit → truncated with a cursor.

Also: `docs/agent/GOAL5-REPORT.md` recorded `corepack pnpm test` as failing at Playwright startup because of a nested Corepack pnpm mismatch; do not touch the packageManager pin — report whichever way the direct `playwright test` invocation goes.
