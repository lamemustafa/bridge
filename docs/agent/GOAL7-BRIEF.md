# Goal 7 — Fourth-round Codex review threads on PR #228 (9 findings, 2026-09-04 13:53 UTC)

Same worktree, branch and rules as `GOAL6-BRIEF.md` (Rust 1.96 on PATH, one regression test per fix, commit per fix, do not push, sealed edits only where `GOAL3B-BRIEF.md` authorises them plus the documented reseal, no won't-fix without file:line counter-evidence). Report as `docs/agent/GOAL7-REPORT.md` in the Goal 6 format with a reply paragraph per thread. Finish with the workspace suite, clippy on `bridge_mcp` with `-D warnings`, the compatibility gate, `corepack pnpm test` for the Node/Vitest part, and Playwright via the direct-vite fallback. All nine are accepted.

1. **`agent_import.rs:369` — corroborate the pre-import mark.** Do not derive the voucher mark from the maximum of a scanned collection (a truncated prefix under-reports it and lets a pre-existing voucher look newer than the mark). At build time, read the company high-water (`ALTVCHID`, `ALTMSTID`) with the same query `changed_since` uses and persist those as the marks with `kind: company_high_water`; if the high-water cannot be observed, refuse to build with `pre_import_mark_unobserved`. Test: a truncated scan cannot produce a mark; a high-water mark classifies a pre-existing voucher as `not_attributable`.
2. **`scripts/package-mcpb.mjs:19` — stage `LICENSE`, `NOTICE`, `THIRD_PARTY_LICENSES_RUST.txt` and the JS inventory into the MCPB archive** alongside the binary, and make the package verifier (or a new check in the script) fail when any is missing from the assembled archive. Test the verifier.
3. **`agent.rs:384` — cap the complete MCP response.** Enforce `BRIDGE_AGENT_MAX_BYTES` on the fully serialised JSON-RPC result, not the inner payload. Keep `content[0].text` to a short human summary (tool, company, row count, truncated flag, evidence state) so the payload appears once, in `structuredContent`. Test: a payload near the cap produces a final message under the cap.
4. **`agent.rs:948` — `ledger_movement` receipt must record the number of ledger rows returned**, captured from the constructed rows before they move into the response. Test with two vouchers over three ledgers.
5. **`agent.rs:520` — omit `party_gstin` from `fields="basic"`** in `ledger_masters`; it belongs to the compliance shape only, and `fields_returned` in the receipt must list exactly what was released. Test both shapes.
6. **`agent.rs:889` — `ledger_not_found`.** When `ledger` is supplied and is absent from the live catalogue (after the existing case- and separator-insensitive normalisation), return a typed error instead of an empty complete result. Test.
7. **`agent.rs:1201` — malformed pagination arguments.** Distinguish absent (default) from present-but-invalid; a negative, non-integer or string `limit`/`offset`/`top` returns `pagination_invalid` from every row-bearing tool. Test the helper and one tool.
8. **`agent.rs:995` — bounded egress-log tail.** Read the last N lines by seeking backward in bounded chunks (e.g. 64 KiB) instead of loading the whole file; cap the tail request at `max_rows`; if the file is larger than a documented ceiling, still serve the tail without reading the head. Test with a file larger than the chunk size.
9. **`agent_import.rs:309` — batch status must ignore unrelated duplicates.** Restrict duplicate detection used for batch status to the batch's own markers and fingerprints; unrelated pre-existing duplicates in the window are reported informationally (`unrelated_duplicates_in_window`) but never turn a fully `posted_verified` batch into `verification_incomplete`. Test.
