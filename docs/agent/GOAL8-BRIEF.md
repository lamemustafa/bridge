# Goal 8 — Fifth-round Codex review threads on PR #228 (12 findings, 2026-09-04 20:17 UTC) plus one housekeeping item

Same worktree, branch and rules as `GOAL7-BRIEF.md` (Rust 1.96 on PATH, one regression test per fix, commit per fix, do not push, sealed edits only where `GOAL3B-BRIEF.md` authorises them plus the documented reseal, no won't-fix without file:line counter-evidence). Report as `docs/agent/GOAL8-REPORT.md` in the Goal 7 format with a reply paragraph per thread. Finish with the workspace suite, clippy on `bridge_mcp` with `-D warnings`, the compatibility gate with the five CI arguments, the Node/Vitest part of `corepack pnpm test`, Playwright via the direct-vite fallback, and one end-to-end run of `node scripts/package-mcpb.mjs`.

1. **`agent_import.rs:684` — verified vouchers must still affect the books.** The verification read fetches `ISCANCELLED` and `ISOPTIONAL`; `posted_verified` requires both explicitly `No`; `Yes` on either yields `posted_not_effective` (counted separately, never verified); a missing flag fails closed with `voucher_accounting_state_not_observed`. Test all three.
2. **`agent.rs:906` — reject movement windows before BOOKSFROM.** If `from` precedes the observed books-from, return `window_precedes_books_from` instead of seeding openings at a later date and calling the result complete. Test.
3. **`agent.rs:395` — egress receipt rows after the byte cap.** Have the cap helper return the surviving row count and record that in `rows_returned`. Test with a cap that removes rows.
4. **`agent.rs:854` — bound unallocated parties.** Return the aggregate (`count`, `amount`) always, plus at most `limit` party rows with `truncated` and `next_offset` on that collection; make the byte-cap trimmer aware of this array as well as `items`/`open_bills` so a large company never loses its totals. Test.
5. **`agent.rs:845` — honour `top` above ten.** `compute_native_outstandings` caps `report.top_parties` at 10 (`crates/bridge-tally-protocol/src/native_outstandings/compute.rs:166`). Build the ranking in `agent.rs` from the uncapped bill-level data the report already carries (party totals from open bills), bounded by `max_rows`; only if the report genuinely does not expose enough to do that, constrain the schema and default to 10 and say so in the description. Record which path you took and why. Test `top: 12` with 12 parties.
6. **`agent.rs:258` — raw company-response evidence.** Expose the raw byte count and SHA-256 of the company-list response from the runtime adapter and use them in `list_companies` and in the identity component of every company-scoped call; stop hashing re-serialised parsed companies. If this touches a sealed file, apply GOAL3B's minimal-edit rule and reseal. Test.
7. **`agent.rs:1383` — malformed optional filters fail closed.** A present non-string `ledger`, `voucher_type`, `as_of`, `direction`, `fields`, `ageing_basis` (any optional string/enum argument) returns `argument_invalid:<name>` rather than widening the read. Test `{"ledger":42}` and `{"voucher_type":[]}`.
8. **`agent.rs:120` — invalid configured limits fail startup.** `BRIDGE_AGENT_MAX_ROWS` and `BRIDGE_AGENT_MAX_BYTES` default only when absent; malformed or out-of-range values fail startup with `limit_setting_invalid:<name>`. Test.
9. **`agent.rs:458` — serialise egress-log appends across processes.** Format the full line first, then append it under the existing `fs2` advisory lock in one write. Test that two interleaved appends produce two parseable lines.
10. **`agent.rs:1766` — keep voucher entries structured while parsing.** Replace the `|`-delimited temporary encoding with typed entry objects accumulated during parsing so a ledger name containing `|` cannot shift fields. Test with a ledger named `A|B`.
11. **`agent.rs:154` — timestamp each stored evidence record** (`read_at` RFC 3339 per record, plus the request duration in milliseconds where the runtime reports it). Test that two reads carry distinct timestamps.
12. **`agent.rs:235` — IPv6 loopback endpoint string.** Use the transport's canonical origin formatter so `::1` renders as `http://[::1]:9000`. Test both loopback forms.
13. **Housekeeping:** add `packaging/mcpb/bin/`, `packaging/mcpb/LICENSE`, `packaging/mcpb/NOTICE`, `packaging/mcpb/THIRD_PARTY_LICENSES.txt` and `packaging/mcpb/THIRD_PARTY_LICENSES_RUST.txt` to `.gitignore` so a local staging run does not dirty the tree, and note it in `packaging/mcpb/README.md`.
