# Goal 15 — Round 14 of Codex review on PR #228 (9 findings, 2026-09-05 21:58 UTC)

Same worktree, branch and rules as `GOAL14-BRIEF.md` (Rust 1.96 via `rustup run 1.96.0`, one regression test per fix, commit per fix, do not push, sealed edits only where `GOAL3B-BRIEF.md` authorises them plus the documented reseal, no won't-fix without file:line counter-evidence). Report as `docs/agent/GOAL15-REPORT.md` in the Goal 14 format with a reply paragraph per thread. Finish with `cargo fmt --check`, the workspace suite (report the main-crate count explicitly), clippy on `bridge_mcp` with `-D warnings`, the compatibility gate with the five CI arguments, the Node/Vitest part of `corepack pnpm test`, and Playwright via the direct-vite fallback. All nine are accepted; the four P1 items go first. Two of them are regressions from Goal 14 and are marked as such.

## P1
1. **`agent_import.rs:1047` — match imported vouchers one-to-one.** On the fingerprint fallback, each observed voucher may satisfy at most one expected voucher: consume matches as they are attributed, and when two expected vouchers share date, type and entries and only one attributable observation exists, mark the second `not_found` (never reuse the observation). If an expected fingerprint is ambiguous within the batch itself, say so in the proof (`ambiguous_within_batch`). Test: two identical expected vouchers, one posted → counts verified 1, not_found 1.
2. **`agent.rs:1634` — exact ledger comparison after resolution** (Goal 14 regression). Once `resolve_ledger_name` has produced the live spelling, filter voucher entries by exact string equality with that spelling, not by the lossy lookup key. Test: live ledgers `AB` and `A-B`, request `AB`, vouchers touching each → only the `AB` voucher is returned.
3. **`agent.rs:806` — validate the window before the ledger filter** (Goal 14 regression). Run `window_honoured` over every parsed row first, then apply the ledger selector; an out-of-window row for any ledger fails with `window_not_honoured`. Same order for the widened corroboration read. Test.
4. **`agent.rs:2651` — changed vouchers missing core fields.** A change-feed row without a non-empty DATE or VOUCHERTYPENAME fails the scan with `change_row_core_field_invalid` before any cursor is built. Test each.

## P2
5. **`agent.rs:1424` — accept `offset` and `limit` for `ledger_movement`** in the dispatch allowlist (Goal 14 omission); add a test that every declared schema property of every tool is in its allowlist, derived from `tool_definitions`, so the two can never diverge again.
6. **`agent.rs:333` — encoded-byte evidence for custom reads.** `post_read` must carry the transport's encoded byte count and SHA-256 (paired-read `Stable { encoded_bytes, encoded_sha256 }`) rather than hashing the decoded string, for vouchers, change feeds, movement, high-water and import verification. If this needs a runtime variant, apply the sealed-edit rule and reseal. Test with a UTF-16 fixture through `vouchers`.
7. **`agent.rs:2294` — company names outside the formula allowlist** (Goal 14 regression from item 16). `SVCURRENTCOMPANY` is an XML element value, not a formula operand: validate with the existing `ValidatedCompanyName` and XML-escape it; keep `TdlStringValue` only for values that actually enter a formula (none remain after Goal 14). Test with a name containing a comma, a plus sign and a non-ASCII character.
8. **`agent.rs:2069` — count every released row collection in outstandings receipts**: open bills plus `unallocated.parties` (and top parties if they are counted as rows elsewhere); document the rule in the receipt helper. Test a wholly unallocated exposure → non-zero rows.
9. **`agent_import.rs:977` — structural truncation detection.** Remove the whole-document `TRUNCAT` substring heuristic. Decision: the completeness evidence for the verification window is (a) the row count below `max_rows`, (b) the identical-set double read, and (c) any *structural* completeness marker the transport or envelope exposes (a documented status/count element), never free text. If no structural marker exists, say so in the report and rely on (a) and (b). Test that a narration containing "truncated payment" no longer blocks verification.
