One file per claimed section number of `TALLY_PROTOCOL_REFERENCE.md`, named for the number.

The rules, and why this shape, are in [`../SECTION-REGISTER.md`](../SECTION-REGISTER.md). In short:
a file records only that a PR intends to use a number. It says nothing about Tally's behaviour —
that is the reference's job, with its confidence markers.
