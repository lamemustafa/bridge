[package]
name = "bridge-tally-primitives"
version = "0.1.0"
description = "Portable Tally value types (exact decimal, validated date) shared below bridge-tally-core"
license = "Apache-2.0"
repository = "https://github.com/lamemustafa/bridge"
publish = false
edition = "2021"
rust-version = "1.96"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
